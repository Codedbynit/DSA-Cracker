sets ---> are the containers that stores unique elements in sorted manner


it is declared :-

sets <int> s;

insertion and deletion

s.insert();
s.erase();

traveral 

s.begin();
s.end();

search

s.find(val) it returns true if sound element else it return s.end();
